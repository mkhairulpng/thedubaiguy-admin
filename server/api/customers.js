const {json, cors, method}=require('./_lib'); const {requireAdmin}=require('./auth/_auth'); const {getPool,ensureSchema}=require('./_db');
module.exports=async function(req,res){cors(req,res);if(method(req)==='OPTIONS')return json(res,204,{});if(!requireAdmin(req,res))return;try{await ensureSchema();const db=getPool();const q=String(req.query?.q||'').trim();const r=q?await db.query(`SELECT * FROM customers WHERE name ILIKE $1 OR email ILIKE $1 OR mobile ILIKE $1 ORDER BY updated_at DESC LIMIT 100`,[`%${q}%`]):await db.query(`SELECT * FROM customers ORDER BY updated_at DESC LIMIT 200`);return json(res,200,{ok:true,data:r.rows});}catch(e){return json(res,e.statusCode||500,{error:e.message});}};
