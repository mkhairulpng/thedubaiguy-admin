const {json,cors,method}=require('./_lib'); const {requireAdmin}=require('./auth/_auth'); const {getPool,ensureSchema}=require('./_db');
module.exports=async function(req,res){cors(req,res);if(method(req)==='OPTIONS')return json(res,204,{});try{await ensureSchema();const db=getPool();if(method(req)==='GET'&&req.query?.mobile&&!requireAdmin(req,res))return; if(!requireAdmin(req,res))return; const r=await db.query(`SELECT id,name,email,mobile,total_spend,order_count,aura_member,aura_balance,aura_cashback_earned FROM customers WHERE aura_member=TRUE OR aura_balance>0 ORDER BY updated_at DESC LIMIT 200`);return json(res,200,{ok:true,data:r.rows});}catch(e){return json(res,e.statusCode||500,{error:e.message});}};
