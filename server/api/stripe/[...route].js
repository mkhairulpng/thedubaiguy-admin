const Stripe=require('stripe');const {json}=require('../_lib');const {requireAdmin}=require('../auth/_auth');const {getPool,ensureSchema}=require('../_db');function client(){if(!process.env.STRIPE_SECRET_KEY){const e=new Error('Stripe is not configured');e.statusCode=503;throw e}return new Stripe(process.env.STRIPE_SECRET_KEY)}module.exports=async function(req,res){const route=String(req.query.route||'').replace(/^\/+|\/+$/g,'');try{const stripe=client();if(req.method==='GET'&&route==='balance'){if(!requireAdmin(req,res))return;return json(res,200,await stripe.balance.retrieve())}if(req.method==='GET'&&route==='payouts'){if(!requireAdmin(req,res))return;return json(res,200,await stripe.payouts.list({limit:Math.min(Number(req.query.limit||10),100)}))}if(req.method==='GET'&&route==='transactions'){if(!requireAdmin(req,res))return;return json(res,200,await stripe.balanceTransactions.list({limit:Math.min(Number(req.query.limit||100),100)}))}if(req.method==='GET'&&route==='payment-summary'){if(!requireAdmin(req,res))return;const charges=await stripe.charges.list({limit:100});const out={total:0,nets:{amount:0,count:0},paynow:{amount:0,count:0},stripe:{amount:0,count:0}};for(const c of charges.data){if(!c.paid)continue;const amount=Number(c.amount||0)/100;out.total+=amount;const meta=String(c.metadata?.pos_payment_method||'').toUpperCase();const type=String(c.payment_method_details?.type||'').toLowerCase();const key=meta==='NETS'?'nets':meta==='PAYNOW'||type==='paynow'?'paynow': 'stripe';out[key].amount+=amount;out[key].count+=1}return json(res,200,out)}if(req.method==='GET'&&route==='summary'){if(!requireAdmin(req,res))return;const[balance,payouts]=await Promise.all([stripe.balance.retrieve(),stripe.payouts.list({limit:5})]);return json(res,200,{available:balance.available,pending:balance.pending,payouts:payouts.data})}if(req.method==='GET'&&route==='products'){if(!requireAdmin(req,res))return;return json(res,200,await stripe.products.list({active:true,limit:100}))}if(req.method==='POST'&&route==='refund'){if(!requireAdmin(req,res))return;const b=req.body||{};if(!b.payment_intent&&!b.charge)return json(res,400,{error:'payment_intent or charge is required'});return json(res,200,await stripe.refunds.create({payment_intent:b.payment_intent,charge:b.charge,amount:b.amount,reason:b.reason}))}return json(res,404,{error:'Unknown Stripe route',route})}catch(e){return json(res,e.statusCode||500,{error:e.message,code:e.code||'STRIPE_ERROR'})}};
