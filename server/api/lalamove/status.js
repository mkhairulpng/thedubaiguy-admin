const {json,cors,method}=require('../_lib'); const {requireAdmin}=require('../auth/_auth'); const {call}=require('./_client'); const {getPool,ensureSchema}=require('../_db');
module.exports=async(req,res)=>{cors(req,res);if(method(req)==='OPTIONS')return json(res,204,{});if(method(req)!=='GET')return json(res,405,{error:'Method not allowed'});if(!requireAdmin(req,res))return;try{const id=String(req.query?.id||'');if(!id)return json(res,400,{error:'id is required'});const result=await call('GET',`/v3/orders/${encodeURIComponent(id)}`);const s=result?.data?.status||null;await ensureSchema();await getPool().query(`UPDATE orders SET lalamove_status=COALESCE($1,lalamove_status),updated_at=NOW() WHERE lalamove_order_id=$2`,[s,id]);return json(res,200,result);}catch(e){return json(res,e.statusCode||500,{error:e.message});}};
